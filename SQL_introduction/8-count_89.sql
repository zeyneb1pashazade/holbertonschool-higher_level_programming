-- Displays the number of records where id is 89 in the table 'first_table'.
-- The COUNT(*) function counts all rows that match the condition specified in the WHERE clause.
SELECT COUNT(*) FROM first_table WHERE id = 89;
